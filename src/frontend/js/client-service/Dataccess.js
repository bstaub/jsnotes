//Basis Datastorage Funktionen hier auslagern




/*
// === Basis Funktionen global start (M=Modell) ===

addNote() {  //saveNoteToLocalStorage()   //AddNote(note)
  //if first entry get empty array [] back!
  const itemArray = this.getItemFromLocalStorage();
  itemArray.push(this.noteObj);
  this.setItemToLocalStorage(itemArray);
}

getNotes() {  //getAllNotesFromLocalStorage  //GetNotes(orderBy, filterBy)
  return this.getItemFromLocalStorage();
}

updateNote(note){   //saveNoteIDToLocalStorage(id)  //this.setItemToLocalStorage(id)

}

getNoteById(id){

}

deleteNoteById(id){

}

// === Basis Funktionen global end ===
*/